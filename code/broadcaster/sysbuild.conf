#==============================================================================
# Sysbuild Configuration
#==============================================================================
# Application-level sysbuild settings go here.
#
# MCUboot board-specific configuration is stored in:
#   sysbuild/mcuboot/boards/<board>/mcuboot.conf
#
# This separation prevents configuration bleed between the MCUboot
# bootloader image and the application image.

# Currently, all sysbuild defaults are provided by the nRF SDK.
# Add overrides below if sysbuild-level (multi-image) configuration is needed.
